---
packages_common:
  - sudo
  - rsync
  - lsof
  - sysstat

packages_ubuntu:
  - "{{ packages_common }}"
  - build-essential

packages_redhat:
  - "{{ packages_common }}"
  - libselinux-python
  - '@Development Tools'
